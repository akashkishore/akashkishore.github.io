import { siteContent } from "./site";
import type { ContactContent } from "./types";

export const contactContent: ContactContent = {
  heading: "Get in touch",
  description: "Email",
  email: siteContent.metaRight.email,
};
